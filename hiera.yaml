---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "OS family"
    path: "os/%{facts.osfamily}/%{facts.operatingsystemmajrelease}.yaml"

  - name: "common"
    path: "common.yaml"
